# back

Deploy-oriented placeholder directory for future backend source split.

Current production `back` service is built from the shared root `Dockerfile`
and the shared application source in the repository root.
