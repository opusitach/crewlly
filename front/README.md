# front

Deploy-oriented placeholder directory for future frontend source split.

Current production `front` service is built from the shared root `Dockerfile`
and the shared application source in the repository root.
