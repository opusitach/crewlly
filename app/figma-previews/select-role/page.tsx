"use client"

import RoleSelectionScreen from "@/components/onboarding/role-selection-screen"

export default function SelectRolePreviewPage() {
  return <RoleSelectionScreen onSelectOwner={() => undefined} onSelectWorker={() => undefined} />
}
